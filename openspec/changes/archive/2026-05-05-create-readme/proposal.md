## Why

`@grammyjs/testing` has no real README — the current file is a leftover TypeScript boilerplate
placeholder with zero relation to the library. Any developer who lands on the npm or GitHub page
sees a blank signal where trust should be built.

## What Changes

- Replace `README.md` entirely with a production-grade, client-facing document
- Add `docs/Y.svg` logo asset (already added by the author) for use in the README header
- No code changes — documentation only

## Capabilities

### New Capabilities

- `readme`: The public-facing README.md for `@grammyjs/testing` — logo, badges, why/problem
  statement, quick-start example, feature list, examples table, documentation placeholder
  (VitePress, coming soon), credits section, and license

### Modified Capabilities

_(none)_

## Impact

- `README.md` — full rewrite
- `docs/Y.svg` — new asset (already present), referenced from README
- No API, dependency, or test changes
